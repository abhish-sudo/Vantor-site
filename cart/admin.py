"""
Cart Admin
Currently no database models for cart (session-based)
Future: Admin for persistent cart models
"""
from django.contrib import admin

# Register cart models here when moving to database-backed carts
